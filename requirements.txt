# Environment for churn_eda_v2.ipynb
# Authored and last run on CPython 3.13.14.
# The floors below are the first releases with Python 3.13 wheels, so keep them
# if you are on 3.13; on 3.10-3.12 anything reasonably recent will do.

pandas>=2.2.3        # 2.2.2 has no 3.13 wheels; DataFrame.map (section 12) needs >=2.1
matplotlib>=3.10.0
seaborn>=0.13.2
openpyxl>=3.1.5      # engine pandas.read_excel needs for .xlsx
jupyterlab>=4.3.0    # to run the notebook; not imported by it
